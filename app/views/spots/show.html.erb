<p id="notice"><%= notice %></p>


<%= image_tag "#{@spot.name}.jpg" %>


<h3>Będzie tu dzisiaj:</h3>

<% @comments.each do |comment| %>
  <%= render comment %>
<% end %>

<%= render partial: 'comments/form' %>

<%= link_to 'Back', spots_path %>
