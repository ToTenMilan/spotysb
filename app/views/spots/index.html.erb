<% @spots.each do |spot| %>
  <div class="col-md-4 spot">

      <%= link_to (image_tag "#{spot.name}.jpg"), spot %>
      <div class="spot-count">
        <%= spot.comments.count %>
      </div>

  </div>
<% end %>






